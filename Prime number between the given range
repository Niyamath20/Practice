#include <stdio.h>

int main()
{
    int start,end,c = 0;
    l:
    scanf("%d %d",&start,&end);
    while(1)
    {
        
        if(start > 0 && end > 0 && start < end)
        {
            for(int i = start; i <= end; i++)
            {
                c = 0;
                for(int j=1;j<=i;j++)
                {
                    if(i % j == 0)
                    {
                        c++;
                    }
                }
                if(c == 2)
                printf("%d ",i);
            }
            return 0;
        }
        else
        {
            printf("Enter valid input : ");
            goto l;
        }
    }
    return 0;
}
